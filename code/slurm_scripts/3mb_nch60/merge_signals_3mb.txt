#!/bin/bash
#SBATCH --job-name=sig_merge_wta_3mb
#SBATCH --account=commons              
#SBATCH --partition=scavenge            
#SBATCH --ntasks=1                
#SBATCH --cpus-per-task=1             
#SBATCH --mem-per-cpu=16G           
#SBATCH --time=00:10:00            
#SBATCH --mail-user=rj65@rice.edu 
#SBATCH --mail-type=ALL            
#SBATCH --output=logs/merging/sig_merge_%j.out       
#SBATCH --error=logs/merging/sig_merge_%j.err 


# Move your execution context safely into scratch
cd $SHARED_SCRATCH/$USER/wta_scratch

# Call your safe, permanent home directory virtual environment
source $HOME/.venv/bin/activate

echo "Starting at : `date`"
echo "Running on hosts: $SLURM_NODELIST"
echo "Running on $SLURM_NNODES nodes."
echo "Running on $SLURM_NPROCS processors."
echo "Current WORKDIR is : $SLURM_SUBMIT_DIR"


# TARGET ALL BATCHES for 
INPUT_FILE="$SHARED_SCRATCH/$USER/wta_scratch/intermediate_output/new_radius/3mb_nch60/premerge/"
OUTPUT_DIR="$SHARED_SCRATCH/$USER/wta_scratch/output/new_radius/3mb_nch60/"

# submit sbatch from the same directory as the .py file
python -u "$SHARED_SCRATCH/$USER/wta_scratch/merge_signals.py" --input "$INPUT_FILE" --outdir "$OUTPUT_DIR"