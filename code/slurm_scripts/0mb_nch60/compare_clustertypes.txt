#!/bin/bash
#SBATCH --job-name=comp_nch_0mb
#SBATCH --account=commons              
#SBATCH --partition=commons            
#SBATCH --ntasks=1                
#SBATCH --cpus-per-task=1             
#SBATCH --mem-per-cpu=32G           
#SBATCH --time=04:00:00            
#SBATCH --mail-user=rj65@rice.edu 
#SBATCH --mail-type=ALL            
#SBATCH --output=logs/comp/comp%j.out       
#SBATCH --error=logs/comp/comp_%j.err 


# Move your execution context safely into scratch
cd $SHARED_SCRATCH/$USER/wta_scratch

# Call your safe, permanent home directory virtual environment
source $HOME/.venv/bin/activate

echo "Starting at : `date`"
echo "Running on hosts: $SLURM_NODELIST"
echo "Running on $SLURM_NNODES nodes."
echo "Running on $SLURM_NPROCS processors."
echo "Current WORKDIR is : $SLURM_SUBMIT_DIR"

if [[ -d $SHARED_SCRATCH/$USER && -w $SHARED_SCRATCH/$USER ]]; then
    cd $SHARED_SCRATCH/$USER/wta_scratch
    source $HOME/.venv/bin/activate

    # target merged signals
    INPUT_FILE="$SHARED_SCRATCH/$USER/wta_scratch/data/0mb/nch60_pt500/"
    OUTPUT_DIR="$SHARED_SCRATCH/$USER/wta_scratch/output/0mb_nch60/"


    python -u "$SHARED_SCRATCH/$USER/wta_scratch/clustertype_comparisons.py" --input "$INPUT_FILE" --outdir "$OUTPUT_DIR"
fi